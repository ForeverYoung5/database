COMMENT ON FUNCTION "util"."read_dataset_length_time_v1_primary_closure"("p_actor" "uuid", "p_plan" "jsonb") IS 'Fresh Length*time primary closure readback: every claimed owner-draft process must hold its claimed desired payload, every claimed read-only flow must still match its declared digest and remain visible, the canonical property and unit group must still bind with the reviewed ratio, and the exact global occurrence closure of the claimed flows must still hold. Reported as counts and booleans only; no foreign payload is returned.';
